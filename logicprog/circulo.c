/*#include "stdio.h"
#include "math.h"
#define _USE_MATH_DEFINES

int main(){
    float raioCirculo, areaCirculo;

    printf("Insira o raio do circulo: ");
    scanf("%f", &raioCirculo);

    areaCirculo = M_PI * pow(raioCirculo, 2);

    printf("A area do cirulo eh: %f", areaCirculo );


    return 0;
}
*/
