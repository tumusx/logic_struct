/*#include "stdio.h"
int main() {
    float precoUnitarioProduto, DinheiroRecebido, Troco, precoTotal;
    int qntdComprada;

    printf("Insira o preco do produto: \n");
    scanf("%f", &precoUnitarioProduto);


    printf("Insira a quantidade que voce deseja comprar: ");
    scanf("%d", &qntdComprada);

    printf("\nInsira a quantidade de dinheiro que foi recebido: ");
    scanf("%f", &DinheiroRecebido);

    Troco = DinheiroRecebido - (precoUnitarioProduto * qntdComprada);
    precoTotal = qntdComprada * precoUnitarioProduto;


    if (DinheiroRecebido >= precoTotal) {
        printf("\nTroco eh %f", Troco);
    }

     else {

        printf("Voce estah devendo %f", Troco);

    }
    return 0;
}*/
